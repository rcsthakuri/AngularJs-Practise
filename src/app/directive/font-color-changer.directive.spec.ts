import { FontColorChangerDirective } from './font-color-changer.directive';

describe('FontColorChangerDirective', () => {
  it('should create an instance', () => {
    const directive = new FontColorChangerDirective();
    expect(directive).toBeTruthy();
  });
});
